apiVersion: 1

providers:
  - name: 'Stargate LLM Gateway'
    orgId: 1
    folder: 'Stargate LLM Gateway'
    folderUid: 'stargate-llm'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
