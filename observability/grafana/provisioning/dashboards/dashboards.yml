# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'Kong LLM Gateway'
    orgId: 1
    folder: 'Kong'
    folderUid: 'kong'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
